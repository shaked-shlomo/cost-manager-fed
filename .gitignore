# Course materials — content_v3.pdf is the lecturer's book and the project
# document asks that it not be shared. Nothing in here is ours to publish.
requirements/

# Working notes and design docs. These carry team ID and mobile numbers.
docs/

# The submission PDF builder carries the team ID and mobile numbers, for the
# same reason docs/ is excluded above. The test scripts beside it stay tracked.
tools/make-pdf.py

# Brainstorming companion scratch files
.superpowers/

# Dependencies and build output
node_modules/
dist/
dist-ssr/
*.local

# Editor and OS noise
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Environment
.env
.env.*
!.env.example

# Local dev-server config for the Claude browser pane, not part of the project
.claude/

# The submission PDF is generated by tools/make-pdf.py, never committed
shaked_shlomo.pdf
