'use strict';

// The four symbols named by the project document. EURO, never EUR.
const SUPPORTED_CURRENCIES = ['USD', 'ILS', 'GBP', 'EURO'];

const DATABASE_NAME = 'costsdb';
const DATABASE_VERSION = 1;

export { SUPPORTED_CURRENCIES, DATABASE_NAME, DATABASE_VERSION };
